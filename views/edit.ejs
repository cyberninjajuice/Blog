<!DOCTYPE html>
  <html lang="en">
  <head>
    <meta charset="UTF-8">
    <style>
      body{
        font-family: 'DINOT-Regular', Verdana;
      }
      main {
        display:flex;
        flex-wrap:wrap;
      }
      h2{
        font-family: Futura, AppleGothic, Verdana, sans-serif;
        font-weight: bolder;
      }
      span{
        font-family: Futura, Verdana, sans-serif;
        font-size: 16px;
        color: rgba(30, 30,30,0.95);
      }
      input{
        font-size:40px;
          background:rgba(240,240,30,1);
          font:2.4em Futura, ‘Century Gothic’, AppleGothic, sans-serif;
          font-size:120%;
          cursor:pointer;
          border-bottom:5px solid rgba(150,150,10,1);
          border-radius:4px;
      }
      input:hover{
        background:rgba(255,200,100,.9);
        border-bottom:2px solid rgba(175,140,60,1);
        border-radius:4px;
      }
      ul{
        padding-left: 20%;
        display:inline-block;

      }
      li{
        margin: 0 auto;
        display: inline-block;
        padding: 30px;
      }
    </style>

    <title> Blog Posts! </title>
  </head>
  <body>
    <h2> PAST: <%-post.title%></h2>
    <img src="<%=post.imageUrl%>">
    <p><%-post.body%></p>
    <ol>
      <form method="POST" action="/post/<%=post.id%>?_method=PUT">
        <li><input type="text" name="title" value="<%-post.title%>"></li>
        <li><textarea rows="10" cols="50" name="body"><%-post.body%></textarea></li>
        <li><input type="text" name="imageUrl" value="<%=post.imageUrl%>"></li>
      </form>  
    </ol>
    <ul>
      <form method="POST" action="/post/<%=post.id%>?_method=DELETE">
      <li><input type="submit" name= "delete" value="Delete this Post">
      </form></li>
      <li><a href="/"> See All Posts</a></li>
    </ul>
  </body>
</html>