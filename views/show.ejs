<!DOCTYPE html>
  <html lang="en">
  <head>
    <meta charset="UTF-8">
    <style>
      body{
        font-family: 'DINOT-Regular', Verdana;
        font-size: 16px;

      }
      main {
        display: flex;
        flex-wrap: wrap;
        margin: 4px 1%;
      }
      h2{
        font-family: Futura, AppleGothic, Verdana, sans-serif;
        font-weight: bolder;
      }
      span{
        font-family: Futura, Verdana, sans-serif;
        font-size: 14px;
        color: rgba(30, 30,30,0.95);
        font-weight: normal;
      }
      input, textarea{
        font-size:40px;
        background:rgba(240,240,30,1);
        font:2.4em Futura, ‘Century Gothic’, AppleGothic, sans-serif;
        font-size:120%;
        cursor:pointer;
        border-bottom:5px solid rgba(150,150,150,1);
        border-radius:4px;
      } input:hover{
        background:rgba(255,200,100,.9);
        border-bottom:2px solid rgba(175,140,60,1);
        border-radius:4px;
      } ul{
        padding-left: 2%;
        display:inline-block;

      } li{
        margin: 0 auto;
        display: inline-block;
        padding: 5px 20px 5px 20px;
      } h5{
        margin:0 auto;
      }
      main p{
        border: yellow, solid, 3px;
        border-radius: 2px;
        margin: 6px;
        font-size: 16px;
        max-width: 80%;
      }
      article{
        border: black, solid, 10px;
        padding:10px;
        margin: 1px;
        border-radius: 3px;
        background: rgba(60, 20, 90, 0.5);
        max-width:99.5%;
      }
      article p{
        font-size:16px;
        max-width:100%;
      }
      h5{
        margin:4px;
        min-width: 100%;
      }
      h4{
        margin: 6px;
        min-width: 100%;
      }
      main h3{ 
        margin: 0 auto;
        min-width: 100%;
      }
      div{
        border: 2px solid rgba(45, 10, 70, 0.25);
        min-width: 80%;
        max-width: 80%;
      }
      div input{
        margin:0 auto;
        padding:2px;
      }
      h4 span{
        margin: 5px;
        padding: 4px 20px 5px 1px;
      }
    </style>

    <title> Blog Post:<%-post.title%> </title>
  </head>
  <body>
    <main>
    <h2> <%-post.title%></h2>
    <img src="<%=post.imageUrl%>">
    <p><%-post.body%></p>
    <h3>Comments</h3>
    <% comments.forEach(function(key){ %>
      <div>    
      <h4><%- key.tag %> <span>  <%= key.updated_at %> - <%- key.author %></span></h4>
      <article>
        <p><%- key.body %></p>
      </article>
      </div>
    <%});%>
    <h3><br>Add your own<br></h3>
        <div>
    <form method="POST" action="/post/<%=post.id%>">
      <ul>
        <li><label>Writer:<br></label>
          <input type="text" placeholder= "Johnny Coltrene" name="author">
        </li>
        <li>
          <label>Tag:<br></label>
          <input type="text" placeholder= "Great Work|Typo|Funny" name="tag">
        </li>
        <li>
          <label>Comment:<br></label>
          <textarea rows="5" cols="65" placeholder="This is a really cool post, but an even cooler WEBSITE!" name="body"></textarea>
        </li>
        <li><input type="submit" value="Send"></li>
      </ul>
    </form>
    </div>
    </main>

    <ul>
      <li><a href="/post/<%=post.id%>/edit"> Edit this Post! </a></li>
      <li><form method="POST" action="/post/<%=post.id%>?_method=DELETE">
      <input type="submit" name= "delete" value="Delete this Post">
      </form></li>
    <li><a href="/"> See All Posts</a></li>
    <li><a href="/post/add"> Add another Post</a></li>
    </ul>
  </body>
</html>